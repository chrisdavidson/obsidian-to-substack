# Substack Conversion Findings

<!-- Generated file. Re-create with: python -m tools.substack_diff --all -->

Generated by `tools/substack_diff`. Each row is a measured difference between
the pipeline's `article.html` and the corresponding published Substack post.

**Generated:** 2026-08-07 23:43  
**Articles compared:** 17  
**Total differences:** 9

## How to read this

A difference means the published post does not match what the pipeline emitted.
Because the author repaired articles by hand in the Substack composer before
publishing, **a difference is a manual fix** — it is the record of work the tool
failed to do.

`vanished in publication` — the pipeline emitted it, the published post lacks it.  
`appeared only in publication (hand-added)` — the author added it in the composer.

> **Caveat 1.** These diffs cannot settle DIAG-01 or FMT-01. Published posts are
> post-repair artifacts, so a clean diff means "nothing needed fixing *or* the fix
> is invisible at construct-count granularity" — not "the pipeline output was
> correct." Those requirements need a live paste of current output.

> **Caveat 2.** Construct counts measure structure, not correctness. Three real
> defects — percent-encoded image paths, stale vault-relative path prefixes, and
> literal inline HTML drawn into rendered tables — produce a *broken image* rather
> than a changed element count, and this diff cannot see any of them. They were
> found by the preflight check and by looking at a rendered PNG.

> **Caveat 3.** 2 article(s) had no archived output and were regenerated with the *current* code, so their rows reflect today's pipeline rather than the code that produced the published post. Their defects may already be fixed. Affected: `Taxonom_Supports_Strategic_Decisions`, `axiom-load-bearing`.

## Confirmed defect patterns

These are the actionable defects. Unlike raw count deltas, each is promoted
only when its specific evidence is present.

| Pattern | Requirement | Articles | What it is |
| --- | --- | ---: | --- |
| **Leading H1 duplicates the post title** | FMT-02 | 4 | `duplicate_title_h1` |
| **Table placeholder comment reached the composer** | TBL-01 | 1 | `table_placeholder_leaked` |

### Leading H1 duplicates the post title

**Requirement:** FMT-02  
**Articles affected:** 4

Pipeline output opens with `<h1>The Essence Statement: The Load-Bearing Identity of What You Build</h1>`, which matches the Substack post title. Substack renders its own title above the body, so this pastes as a duplicate heading the author must delete by hand.

Affected articles:

- `essence-statement`
- `propositions-axiom-relationship`
- `saas-cost-benefit-data-ownership`
- `strip-the-brand-names-an-introduction-to-first-principles-thinking-in-regulated-software`

### Table placeholder comment reached the composer

**Requirement:** TBL-01  
**Articles affected:** 1

Pipeline output contains 1 table placeholder comment(s) and 0 rendered table(s). The published post has 0 table(s). Example: `TABLE 1: See table-1.csv for Datawrapper import`

Affected articles:

- `propositions-axiom-relationship`

## Differences by family

Raw construct-count deltas. Some are editorial changes the author made while
publishing, not tool defects — the pattern section above is the actionable list.

| Family | Differences |
| --- | --- |
| FORMATTING | 5 |
| TABLES | 4 |

## Frequency by construct

Drives fix order — most-affected construct first.

| Construct | Family | Articles affected |
| --- | --- | --- |
| `headings` | FORMATTING | 5 |
| `html_comments` | TABLES | 4 |

## Per-article detail

### Taxonom_Supports_Strategic_Decisions

No construct-level differences detected.

### a-taxonomy-for-innovation-giving-new-ideas-a-structural-home

No construct-level differences detected.

### axiom-load-bearing

No construct-level differences detected.

### essence-statement

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `headings` | FORMATTING | 7 | 6 | -1 | count changed |

### measuring-success-how-to-track-progress-toward-done

No construct-level differences detected.

### propositions-axiom-relationship

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `headings` | FORMATTING | 7 | 6 | -1 | count changed |
| `html_comments` | TABLES | 1 | 0 | -1 | vanished in publication |

### saas-cost-benefit-data-ownership

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `headings` | FORMATTING | 9 | 8 | -1 | count changed |

### strip-the-brand-names-an-introduction-to-first-principles-thinking-in-regulated-software

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `headings` | FORMATTING | 8 | 7 | -1 | count changed |

### taxonomy-drift-the-silent-decay-of-shared-language

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `html_comments` | TABLES | 2 | 0 | -2 | vanished in publication |

### taxonomy-the-language-breaks-first

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `html_comments` | TABLES | 2 | 0 | -2 | vanished in publication |

### the-agent-and-the-taxonomy-why-autonomous-systems-need-shared-language

No construct-level differences detected.

### the-knowledge-base-is-already-written

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `headings` | FORMATTING | 9 | 10 | +1 | count changed |

### the-ladder-you-cannot-skip-a-taxonomy-maturity-model-for-organization-that-mean-it

No construct-level differences detected.

### the-roi-of-shared-language-measuring-taxonomys-business-value

No construct-level differences detected.

### the-silent-override-when-systems-reshape-the-taxonomy-they-were-built-to-carry

| Construct | Family | Pipeline | Published | Delta | Reading |
| --- | --- | ---: | ---: | ---: | --- |
| `html_comments` | TABLES | 2 | 0 | -2 | vanished in publication |

### training-an-ai-model-with-taxonomy-and-ontology

No construct-level differences detected.

### when-taxonomies-fail-five-patterns-of-structural-collapse

No construct-level differences detected.

## Hand-recorded findings

### The stripped H1's text never reached the output

**Requirement:** FMT-02

The head title of `output/torture-test/article.html` was the filename slug
(`torture test`) while its source H1 reads `# Torture Test: Every
Construct`; the head title of
`output/propositions-axiom-relationship/article.html` was likewise the slug
(`propositions axiom relationship`) while its source H1 reads
`# Propositions: How One Axiom Becomes a Framework`. Both files contain
zero `h1` elements — correct, that is the FMT-02 strip working.
`metadata.json` for `torture-test` held only `tags` and `series`, no title
key at all.

Root cause: `strip_duplicate_title` extracted the dropped heading's text
only to write a log line, then discarded it, so the filename-slug fallback
fired for every article regardless of whether it had a real title.

Fix: `strip_duplicate_title` now returns the dropped text; `convert_article`
resolves one title (H1, then frontmatter title, then slug) and feeds it to
the head title element, `metadata.json`, the Datawrapper chart title, and a
new Title line in the CLI's success output.

Note: Substack's own title field is still filled by hand — this fix
supplies the text the author needs to paste, not the paste itself.

### Step 3's command never entered the Datawrapper code path

**Requirement:** TBL-05

The step-3 command in `docs/MORNING-CHECKLIST.md` omitted the
`--datawrapper` flag, so `dw_token` stayed `None` and the Datawrapper branch
in `convert_article` was never entered. Consequently
`output-dw/propositions-axiom-relationship/` is an ordinary conversion — it
holds `table-1.png` with no iframe and no Datawrapper URL. The run produced
zero information and TBL-05 remains fully open.

Fix: the `--datawrapper` flag has been added to the command in
`docs/MORNING-CHECKLIST.md`. The author decides whether to re-run it;
nothing in this change re-runs it.
